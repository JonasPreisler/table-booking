<p id="notice"><%= notice %></p>

<h1>Guestlists</h1>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @guestlists.each do |guestlist| %>
      <tr>
        <td><%= link_to 'guestlist', guestlist %></td>
        <td>-</td>
        <td><%= link_to 'Edit', edit_guestlist_path(guestlist) %></td>
         <td>-</td>
       <td><%= link_to 'Delete', guestlist, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Guestlist', new_guestlist_path %>
